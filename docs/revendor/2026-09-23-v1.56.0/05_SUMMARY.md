# 05 — Summary: LibKa0s v1.55.0 -> v1.56.0

Plan item RV-PC, 2026-09-24, branch `feat/2026-09-23-review-audit-remediation`. Written by hand from
the WA-01-amended local `revendor-libka0s.md`. Steps 0 and 2 to 4 only. Nothing pushed, and the
addon version is not bumped.

## The tag, and the per-file minors

`v1.55.0` -> `v1.56.0` (tag object `4622018`, commit `514fc0a`). The base comes from the `CLAUDE.md`
provenance line (v1.55.0), and the payload matched that tag byte for byte before the copy. Fifteen
files move a minor: Core 8, Lifecycle 2, Bus 2, Schema 2, Item 2, Media 4, Widgets 10, DebugLog 13,
Slash 15, Launcher 2, Options 24, OptionsWidgets 31, OptionsTabs 4, OptionsScroll 4 and Perf 13. No
file is added or removed, and there is no skew. The kit revision goes from 25 to 26 (three new files:
`asserts.lua`, `mock_events.lua`, `prose_lists.lua`). The table and the diffs are in `01_DELTA.md`,
sections 3c to 3f.

No span bundle is written and no base correction is recorded. Step 0 is ok. The 29 unrecorded tags
3h lists are PC-25's.

## Delivered on the re-vendor, with nothing asked for

- A refused `/pc set` or `/pc reset` now prints the write seam's refusal instead of echoing the old
  value (Slash 15).
- `CreateOptionsPanel` parks in combat and replays at `PLAYER_REGEN_ENABLED` (Options 24).
  `OpenOptionsPanel` answers a boolean.
- The debug console's buffer trims in batches at the cap, not once per line (DebugLog 13).
- The launcher's missing-library notice prints once, without the `[LibKa0s] ` prefix (Launcher 2).
- `Core.Format` survives a secret value in a numeric slot (Core 8). `RegisterLSM` counts only what
  LSM holds (Media 4).
- Kit 26's stricter fakes: frames start shown, the AceDB fake raises on a bad profile name, a
  recording `EventRegistry`, `__badEvents`, the lone-CR gate, the store-root prose files, and case
  names carrying the section sign.

## Contract blockers

None in the library (`01_DELTA.md` 3g). The contract changes that turn existing pins red are listed
below as suite reds. They are not fixed in this copy-only commit.

## Adopted, declined

Nothing adopted or declined here. The opt-ins (Core's `SafeRegisterEvent` family, Schema's `SetMany`
/ `normalize` / `writeThrough`, Launcher's `isEnabled`, `RenderTabbedSchema`'s `opts`) are taken or
left by this addon's M3 items: PC-01, PC-02, PC-05 and PC-12.

## Gates at the RV commit

| Suite | Result |
|---|---|
| `ka0s-bounded luacheck .` | 0 warnings / 0 errors in 48 files |
| `ka0s-bounded lua5.1 tests/run.lua` | 435 passed, **3 failed**, 0 skipped, 438 total |
| `diff -r` of both payloads against the tag | empty |

The three reds, each predicted by the library's release dry-run:

1. *the Schema stub carries the whole live surface, library and instance* (`SetMany` missing).
   Cleared by PC-01.
2. *a settings page shown in combat is covered, not drawn and not closed* (the Options 24 park). No
   M3 item names it. It must be green by PC-DOCS.
3. *the buffer is capped and drops its oldest lines first* (DebugLog 13's batched trim: 1520 held
   against 1500). No M3 item names it. It must be green by PC-DOCS.

`docs/test-cases.md` and the README test badge are regenerated by a later item (PC-DOCS), never by
this commit.

## Open

- Reds 2 and 3 have no named M3 item. They fall to PC-DOCS at the latest, unless the plan assigns
  them earlier.
- PC-25's tag count (26) disagrees with the 3h listing (29). PC-25 should re-derive it.
